---
title: Test result
---

## Test result

Giờ thì mình sẽ dùng lệnh `curl` để kiểm tra kết quả, và `jq` để format lại về JSON

```bash
curl http://localhost:8000 | jq
```

<Image src="/assets/deploy-nodejs/4.2.1.png" />

Có thể thấy là ứng dụng NodeJS của chúng ta giờ đã hoạt động.
